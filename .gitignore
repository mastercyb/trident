# Rust build artifacts
/target/
editor/zed/target/

# Compiled TASM output (but keep hand-written baselines)
*.tasm
!*.baseline.tasm
# *.neural.tasm are auto-generated by `trident bench`

# Compiled WASM grammars
*.wasm

# Node.js
**/node_modules/

# Tree-sitter build
**/build/

# Tree-sitter grammar repos (managed separately)
editor/tree-sitter-trident/
editor/zed/grammars/

# OS
.DS_Store

# Neural weight artifacts — too large for git (v2 checkpoint ~30MB)
data/neural/

# Agent memory — keep plans/audits/reports, ignore scratch
/.cortex/scratch/

# Rust build artifacts
/target/
editor/zed/target/

# Compiled TASM output (but keep hand-written baselines)
*.tasm
!*.baseline.tasm
!*.neural.tasm

# Compiled WASM grammars
*.wasm

# Node.js
**/node_modules/

# Tree-sitter build
**/build/

# Tree-sitter grammar repos (managed separately)
editor/tree-sitter-trident/
editor/zed/grammars/

# OS
.DS_Store

# Neural optimizer weight artifacts (but keep bundled model in data/)
**/neural/weights.bin
**/neural/meta.toml
!data/neural/weights.bin
!data/neural/meta.toml

# Agent memory — keep plans/audits/reports, ignore scratch
/.cortex/scratch/

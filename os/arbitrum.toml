# Arbitrum (Stylus) — EVM-compatible L2 with Wasm smart contracts
# https://arbitrum.io

[os]
name = "arbitrum"
display_name = "Arbitrum (Stylus)"
vm = "wasm"

[runtime]
binding_prefix = "ext.arbitrum"
account_model = "account"
storage_model = "key-value"
transaction_model = "signed"

[cross_chain]
protocols = ["ethereum-l2"]

[status]
level = 1
ext_modules = 0
tests = false
notes = ""

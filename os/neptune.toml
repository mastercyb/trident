# Neptune — provable blockchain powered by Triton VM
# https://neptune.cash

[os]
name = "neptune"
display_name = "Neptune"
vm = "triton"

[runtime]
binding_prefix = "ext.neptune"
account_model = "utxo"
storage_model = "merkle-authenticated"
transaction_model = "proof-based"

[cross_chain]
protocols = []

[status]
level = 2
ext_modules = 6
tests = false
notes = "kernel, proof, recursive, registry, utxo, xfield"

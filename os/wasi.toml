# WASI — WebAssembly System Interface
# Target: wasm

[os]
name = "wasi"
display_name = "WASI"
vm = "wasm"

[runtime]
binding_prefix = "ext.wasi"
account_model = "process"
storage_model = "filesystem"
transaction_model = "syscall"

[cross_chain]
protocols = []

[status]
level = 1
ext_modules = 0
tests = false
notes = ""

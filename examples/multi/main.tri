program multi

use helpers

fn main() {
    let a: Field = pub_read()
    let doubled: Field = helpers.double(a)
    pub_write(doubled)
    let sq: Field = helpers.square(a)
    pub_write(sq)
    // Test module constant resolution
    pub_write(helpers.MAGIC)
}
